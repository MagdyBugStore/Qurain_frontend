# ملف إعدادات Render (اختياري)
# يمكن استخدامه لتكوين النشر تلقائياً

services:
  - type: web
    name: quran-online
    env: static
    buildCommand: npm run build
    staticPublishPath: ./out
    envVars:
      - key: NODE_ENV
        value: production
